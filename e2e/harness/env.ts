/** Адреса прогона — и только они.
 *
 *  Файл нарочно без единого побочного действия: его подключают и настройка
 *  Playwright, и сами тесты. Настройка загружается ЗАНОВО в каждом рабочем
 *  процессе, поэтому всё, что делается при подключении (создать каталог,
 *  стереть базу), выполнилось бы посреди прогона — у уже запущенного сервера
 *  из-под рук исчезал файл базы, и SQLite отвечал «база только для чтения».
 *  Разобраться в причине по такому сообщению почти невозможно.
 */

/** Порты нарочно нестандартные: чтобы прогон не подрался с приложением,
 *  запущенным вручную для разработки (8000 у бэкенда, 4173 у vite preview). */
export const API_PORT = 8010;
export const APP_PORT = 4180;
export const API_URL = `http://127.0.0.1:${API_PORT}`;
export const APP_URL = `http://127.0.0.1:${APP_PORT}`;
