# Принятые находки поиска секретов — по одному отпечатку на штуку.
#
# Отпечаток = коммит:файл:правило:строка. Это НЕ отключение правила и не
# исключение файла: любая другая находка того же вида в любом другом месте
# по-прежнему уронит сборку. Так и должно быть.
#
# ЧТО ЗДЕСЬ И ПОЧЕМУ.
#
# В коммите c11f5f8 («HIGH: рабочий токен доступа лежал в журнале открытым
# текстом») я привёл в пояснении настоящую строку журнала — ту самую, ради
# которой правка и делалась. В ней стоял поддельный токен, но выглядел он как
# настоящий: начинался с eyJhbGciOiJIUzI1NiJ9 — это обычный заголовок JWT в
# base64. Поиск секретов честно на него сработал (правило generic-api-key).
#
# Токен был выдуман от начала до конца: подпись «podpis», тело
# «SEKRETNYJ_TOKEN». Ни одного настоящего ключа в репозитории нет и не было.
#
# В рабочем дереве строка уже заменена на «<здесь стоял настоящий токен
# целиком>» — новые коммиты чистые. Но сканер идёт по ВСЕЙ истории (в
# security.yml так и написано: «удалить файл мало, коммит остаётся»), а
# переписывать историю ради этого нельзя: это force push по общей ветке.
#
# Отсюда правило на будущее: примеры в пояснениях не должны выглядеть как
# ключи. Настоящий пример стоит того, но не ценой слепого сканера.
c11f5f81ab4c4a58ea4275f0243e373b56cb7867:backend/app/main.py:generic-api-key:51
c11f5f81ab4c4a58ea4275f0243e373b56cb7867:backend/tests/test_log_redaction.py:generic-api-key:7
