# StaffSwipe — что готово и что осталось подключить

Проверка 10–13 сентября 2026. База изменений: `c58584932c23002a96f56780cd00f676064f283d`, ветка `codex/staffswipe-hospitality-design`.

## Результат

Обновлён существующий React/FastAPI проект: старт, лента двух ролей, детали, галерея, профиль, заголовки рабочих страниц, создание смены в три шага, текущие смены/история, помощь и отзыв. Сохранены реальные API, чат, приглашения, комиссия, документы и админка.

Выполненные исправления:

- У кнопок сохраняются базовые стили при передаче дополнительного класса. Нажатия внутри свайп-карточки доходят до галереи, сохранения и подробностей.
- Лента адаптирована под низкие экраны; условия не перекрываются кнопкой отклика. Нижние отступы учитывают безопасную область Telegram и устройства.
- До пяти собственных фотографий сотрудника проходят серверную проверку источника. Можно удалить фото и выбрать главное. Загрузка не допускает сохранения незавершённого файла.
- Запоздавшие ответы предыдущего аккаунта не меняют новую сессию. Одновременные ошибки авторизации используют один повторный вход. Временная потеря связи не выдаётся за выход из аккаунта.
- Повторное нажатие при регистрации блокируется; вход на реальный сервер требует Telegram. Демо явно обозначено и использует вымышленные данные.
- Некорректная подпись Telegram безопасно отклоняется. WebSocket не принимает токен доступа к документам и прекращает использование просроченного токена.
- Сбой создания платежа возвращает ошибку сервиса. Приложение не показывает вымышленную платёжную ссылку.
- Исправлено отображение календарных дат и текстов расчёта. Отзыв отправляется отдельной кнопкой после выбора оценки.

## Проверки

| Проверка | Результат | Что это подтверждает |
| --- | --- | --- |
| Backend, Ruff | Пройдено | Статические проверки Python. |
| Backend, pytest | **554 пройдено, 1 пропущен** | API, права/сессии, смены, расчёт, документы, галерея, отказ оплаты, черновики, приглашения и доступ единственного владельца. |
| Frontend, ESLint / TypeScript | Пройдено | Нет ошибок линтера и типов в изменённом приложении. |
| Frontend, Vitest | **163 пройдено, 30 файлов** | Сессия, галерея, карточка, дата, кнопки, регистрация, отзыв, подтверждение пополнения, отказ/сбой/offline в админке. |
| Сквозные Playwright | **51 пройдено** | Регистрация двух ролей, публикация с предпросмотром, черновики/потеря ответа, журнал приглашений, чат/WS, подтверждение, код, завершение, комиссия/PDF/админка, боковые safe area и подтверждение пополнения. |
| Мобильная компоновка | Пройдены проверки 9 размеров от 320×568 до 430×932 | Перекрытия, горизонтальная прокрутка, клавиатура в чате, панели, светлая/тёмная тема и увеличенный интерфейс. |
| Поворот и планшет | Пройдены дополнительные 844×390 и 1024×768, а также 320×568 с боковыми insets | Содержимое, нижняя навигация и кнопки шторки не уходят за безопасные края. |
| Визуальная сверка | Сохранены пары «референс / приложение» | Старт, лента, смены, чат и профиль на 390×844; дополнительный просмотр 320×568. |
| Роль заведения, дополнительный проход | 3 сквозных сценария пройдено; мастер также пройден вручную | Компактный выбор должности, сохранение условий при возврате, предпросмотр, публикация и появление в списке. Добавлены снимки источников 21 и 24–27. |

Актуальные GitHub Actions и точный проверенный SHA находятся в [PR #54](https://github.com/polompol/sraffswipe1/pull/54). Результат предыдущего коммита не подменяет проверку новых изменений.

Сквозные тесты работают с настоящим FastAPI и изолированной SQLite-базой. Вход там технический, только для теста. Это не проверка реального аккаунта Telegram, банковского платежа, боевого Postgres или боевого S3.

Один backend-тест с настоящим Redis Pub/Sub пропущен: в среде проверки нет `redis-server`. Проверки с заменителями Redis выполнены. В backend-прогоне есть предупреждения зависимостей; они не приравниваются к ошибкам тестов.

Сквозные прогоны необходимо выполнять отдельно от других сборок. Ранее одновременный запуск затронул `tma/dist` и каталог следов Playwright; результат с ошибкой отсутствующего файла trace не используется как итоговая проверка. Также исправлена зависимость старого теста переноса смен от часового пояса машины: «вчера» считается по календарю смен, как в приложении.

## Перед запуском на реальных пользователях

| Подключение | Конкретный результат |
| --- | --- |
| Домен и приложение | Разместить проверенную ветку на выбранном HTTPS-адресе; собрать frontend с `VITE_USE_BACKEND=true` и правильным API URL. |
| Telegram | Указать адрес Mini App в настройках своего бота, проверить получение и серверную валидацию `initData`, возврат, клавиатуру, safe area на реальных iOS/Android. |
| Backend | Поднять Postgres, применить миграции; проверить API и сохранение данных после перезапуска. |
| Redis и планировщик | Проверить обмен сообщениями между процессами, ограничения запросов, напоминания и автоматическое закрытие смен. |
| Фотографии | Подключить собственное S3-совместимое хранилище; пройти загрузку → просмотр → изменение главного фото настоящим профилем. |
| Проверка компании | Настроить DaData, проверить успешный и ошибочный ИНН. Самозанятость и медкнижка не становятся проверенными от одного заполнения поля. |
| Оплата комиссии | Подтвердить применимость выбранного способа оплаты к комиссии сервиса по правилам Telegram. Затем настроить провайдера, проверить создание платежа, возврат, подтверждённый webhook, повторную доставку и обновление баланса. |
| Владелец админки | Задать в серверном `ADMIN_TG_IDS` ровно один положительный числовой ID владельца. Его фактическое значение пока не предоставлено и не настроено. Без него production не запускается. |
| Поддержка | Указать реальный `VITE_SUPPORT_URL`; владелец отправляет тестовую жалобу на тестовую смену и проверяет разбор в админке. |
| Документы сервиса | Заполнить оставшиеся реквизиты/контакты оператора в существующих `docs/legal/`; проверить опубликованные страницы. |

Telegram отдельно описывает поддержку темы, безопасных областей, кнопки возврата и проверку переданных данных в [официальной документации Mini Apps](https://core.telegram.org/bots/webapps). Проверка браузерных размеров не заменяет этот последний прогон в приложениях Telegram.

Никаких новых платных подписок для этой доработки не оформлено. Higgsfield запросил тариф Basic или выше; для собственных демонстрационных изображений использован доступный инструмент генерации. Рабочее размещение и услуги провайдеров зависят от выбранной инфраструктуры: их стоимость и доступность этим прогоном не проверялись.

## Правила, которые нельзя подменять дизайном

Оплату за работу сотрудник получает непосредственно от заведения. Комиссия StaffSwipe 10% начисляется заведению; она не вычитается из показанной сотруднику ставки. Баланс в профиле заведения предназначен для комиссии. Резервирование полной оплаты труда из экрана 43 исходного примера не реализовано.

Код прихода знает заведение, сотрудник получает его на месте и вводит в приложении. Это доказательство присутствия, а не гарантия оплаты и не автоматический таймер фактических часов.

После окончания смена может закрыться, если обе стороны подтвердили выход. Планировщик также закрывает подходящие смены через 12 часов после окончания по текущим правилам сервера; спорные и явно несостоявшиеся смены требуют другой обработки. Подтверждение сотрудника в существующей модели влияет на автоматический расчёт даже при незавершённом подтверждении заведения. Дизайн не меняет эти правила. Полное двухстороннее принятие табеля из примера потребует отдельного изменения модели и тестов.

Все 50 экранов сопоставлены с реальными возможностями в [карте UX](STAFFSWIPE-UX-50.md). Серверные черновики и базовый журнал исходящих приглашений реализованы. Кабинет обращений, личные блокировки, адресные приглашения на конкретную смену и новый табель остаются отдельными доработками.

## Воспроизведение

После установки зависимостей выполнить `bash scripts/verify.sh`. Сквозные тесты запускать отдельно из `e2e` командой `npm test` с Python backend-окружения в `PATH`; не запускать одновременно другую сборку `tma/dist`.

Для просмотра без серверных ключей: `npm --prefix tma ci`, затем `npm run dev` из корня. Это демонстрационный режим. Для рабочего размещения использовать существующие инструкции `docs/DEPLOY.md` и фактические настройки окружения; старые оценки стоимости/времени в этих инструкциях не перепроверялись.

## Черновики и исходящие приглашения

Неполную смену можно сохранить кнопкой «Сохранить и выйти» на любом шаге, продолжить в другом сеансе и удалить с подтверждением. Черновики доступны только владельцу и не попадают в публичную ленту. Версия защищает свежие изменения от перезаписи другим устройством; ошибка сохранения оставляет поля в форме. Это явное сохранение, не фоновое автосохранение.

Публикация использует сохранённую версию и серверную квитанцию в одной транзакции. При потере ответа кнопка «Проверить публикацию» возвращает уже созданную смену. Повтор запроса не создаёт вторую публикацию. Применяются прежние проверки условий, фото, комиссии и модерации. Требуется миграция `f5a6b7c8d9e0` после `e4f5a6b7c8d9`.

Журнал `/invitations` показывает исходящий интерес заведения к сотрудникам: «Все», «Без ответа», «Ответили», пагинацию, реальные статусы договорённостей, чат и историю выбранного сотрудника. Приглашение до взаимного отклика не привязано к одной смене. Отметки прочтения и доставки не выдумываются. Повторное приглашение корректно меняет прежний отказ и учитывает уже существующий отклик сотрудника.

В рабочем режиме данные хранятся через API. Локальное хранение черновиков используется только в явно обозначенном демо.

## Удобство оплаты, адаптация и доступ владельца

Боковые safe-area отступы, которые передаёт подключённый Telegram SDK, учитываются страницами, навигацией, шторками, уведомлениями и полем чата. Дополнительно проверяются 320×568, горизонтальный 844×390 и планшетный 1024×768 с увеличенным интерфейсом. Это эмуляция размеров и входных значений safe area, не физическая проверка всех моделей телефонов. Клавиатура чата использует существующую обработку `visualViewport`; реальный Telegram iOS/Android остаётся обязательным smoke-тестом.

Выбор суммы пополнения сначала открывает шторку с суммой, назначением денег, кнопками «Перейти к оплате» и «Отмена». Отмена не создаёт платёж. После открытия провайдера можно повторно открыть ту же ссылку и проверить баланс; возврат из банка не рисует статус «Оплачено». Ссылка сохраняется только в текущем открытом компоненте: полноценного журнала незавершённых банковских платежей здесь нет. Денежные операции этим браузерным прогоном не выполнялись.

Для цифровых товаров и услуг внутри Telegram требуются [Telegram Stars](https://core.telegram.org/bots/payments-stars); для физических товаров и услуг Telegram описывает [платежи через провайдеров](https://core.telegram.org/bots/payments). Классификация комиссии StaffSwipe этим изменением не подтверждена. Существующая интеграция ЮKassa не объявляется автоматически подходящей для продажи любых цифровых функций; до запуска нужно подтвердить схему и правила возврата.

В production `ADMIN_TG_IDS` принимает только один ID; пустота, ноль, отрицательное значение, username или список не проходят конфигурационную проверку. Каждый запрос админки проходит серверную авторизацию, а не проверку скрытой кнопки. Подмена настроек на список закрывает доступ даже при существующем JWT. Локальный DEV-режим с тестовым ID 0 нельзя включать на рабочем сервере. Сетевой сбой в панели показывает повтор запроса, настоящий 403 — отказ в доступе.

Это не обещание абсолютной защиты при захвате Telegram-аккаунта, серверных секретов или инфраструктуры. До запуска владельцу следует включить двухэтапную проверку Telegram, проверить активные сеансы, защитить доступ к серверу и настроить резервные копии. Пароль и токен бота через переписку для привязки админки не нужны.
