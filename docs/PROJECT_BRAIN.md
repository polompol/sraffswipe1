# StaffSwipe — Project Brain

> Канонический контекст проекта для продолжения работы между чатами, моделями и сессиями.
> Последняя синхронизация: 2026-09-14; рабочая ветка `codex/staffswipe-release-candidate`.

## Зачем этот файл

Если запрос звучит как «Продолжи StaffSwipe», «работаем дальше», «доделай проект» или пользователь возвращается к проекту после паузы — **не начинать заново**. Сначала восстановить состояние по этому файлу и актуальному `main`, затем продолжить с ближайшей незавершённой задачи.

## Приоритет источников истины

1. Актуальный код и миграции в `main` — фактическое поведение.
2. Этот файл — продуктовый checkpoint и правила продолжения.
3. `CLAUDE.md` — подробная техническая память и доменные инварианты.
4. `DESIGN_SYSTEM.md`, `SECURITY.md`, `README.md` и релевантные документы в `docs/`.
5. Свежая явно подтверждённая пользователем договорённость — для продуктовых решений.

Если источники расходятся, не угадывать. Указать конфликт и опереться на более свежий исполняемый код для технических фактов.

## Что такое StaffSwipe

Telegram Mini App для HoReCa: двусторонний marketplace сотрудников и заведений в логике swipe → взаимный интерес → match → подтверждение смены → фактический выход → завершение/спор → репутация и комиссия.

Основные роли:
- сотрудник / соискатель;
- заведение / работодатель.

Продукт должен ощущаться очень простым, быстрым и визуально лёгким, как swipe-приложение, но оставаться серьёзным рабочим инструментом для выхода на смены.

## Техническая база

- `tma/`: React 18 + TypeScript + Vite + `@tma.js/sdk-react` v3, TanStack Query, Zustand, HashRouter.
- `backend/`: FastAPI + SQLAlchemy 2 + Pydantic v2 + Alembic.
- PostgreSQL в production, SQLite для dev/tests.
- `backend/bot/`: aiogram bot как отдельный процесс.
- Production: `docker-compose.prod.yml` + Caddy HTTPS + PostgreSQL + API + bot + SPA.
- E2E: Playwright в `e2e/`.

## Критичные доменные инварианты — не ломать

Подробное обоснование хранится в `CLAUDE.md` и профильных документах.

- Молчание после смены не должно позволять заведению избежать комиссии.
- Для расчёта достаточно подтверждения сотрудника в предусмотренных текущей логикой случаях.
- Код прихода — доказательство выхода, а не самостоятельный финансовый триггер.
- Комиссия — 10% и начисляется только за закрытую смену, идемпотентно по `match_id`.
- Баланс и финансовые списания меняются только атомарными операциями с условием; никаких read-modify-write для денег.
- Идентичность пользователя основана на Telegram `tg_id`; relink не должен допускать эскалацию до администратора.
- Планировщик является частью бизнес-логики: без settlement-задач смены не закрываются и комиссия не начисляется.

## UX и дизайн

- Mobile-first и Telegram Mini App-first.
- Учитывать safe-area, viewport, Telegram theme, BackButton/MainButton, haptics и появление клавиатуры.
- Минимализм важнее декоративной сложности.
- Сохранять текущую дизайн-систему: багровый `--gold:#a51c30`, слоновая кость `--bg:#efe7d3`; новые цвета только через токены в `tma/src/theme/theme.css`.
- Каждый экран/действие проектировать не только для happy path: loading, empty, error, disabled, success, retry, destructive confirmation.
- Не создавать случайный новый визуальный язык для каждого экрана.
- Тексты UI — простые, тёплые, по-русски.

## Безопасность и privacy

- Административные права проверяются на сервере, а не в клиенте.
- Секреты только через environment variables; production safety guards не ослаблять.
- Не хранить фото документов/медкнижек; хранить только необходимые статусы.
- ИНН, телефон и другие чувствительные данные не отдавать в публичных лентах/эндпоинтах.
- Каждую новую функцию до завершения проверить с позиции злоумышленника: что можно накрутить, обойти, повторить, подобрать или использовать для эскалации прав.
- Финансовые webhook/операции должны быть идемпотентными и проверять сумму/метаданные.

## Проверки после изменений

Backend:
```bash
cd backend
ruff check . && python -m pytest -q
```

TMA:
```bash
cd tma
npm run lint && npx tsc --noEmit && npx vitest run && npm run build
```

Всё сразу:
```bash
bash scripts/verify.sh
```

Сквозные проверки:
```bash
bash scripts/e2e.sh
```

Новые поля моделей требуют Alembic migration; цепочка ревизий должна оставаться с одним head.

## Текущее состояние

Полная готовность к пилоту **пока не подтверждена**. Прежняя запись «вся работа слита» устарела: дизайн и дополнительные исправления находятся в PR #54 и #59. Итоговая интеграция готовится в `codex/staffswipe-release-candidate`, без выкладки и реальных финансовых операций. Помимо внешних настроек остаются подтверждённые программные задачи по сообщениям и интерфейсу.

При этом продуктовая работа продолжается: приоритет — довести UX и визуальную систему до полноценного, очень удобного Telegram Mini App без разрушения уже готовой доменной и финансовой логики.

Основные направления следующего слоя:
1. Полный UX обеих ролей: onboarding → регистрация → профиль → лента/swipe → смена → match → подтверждение → завершение/спор → рейтинг → уведомления → настройки/поддержка.
2. Единая система компонентов, кнопок, анимаций и состояний.
3. Адаптация под разные мобильные размеры и Telegram WebView.
4. Trust & Safety: жалобы, no-show, фейковые заведения, мультиаккаунты, блокировки, аудит.
5. Админка с серверной авторизацией, аудитом и минимальными привилегиями.
6. Наблюдаемость, аналитика продуктовой воронки и проверяемый production flow.

## Протокол «Продолжи StaffSwipe»

Перед существенной работой:
1. Прочитать этот файл.
2. Проверить актуальный `main` и релевантные файлы к задаче.
3. Определить, что уже существует и что реально является следующим шагом.
4. Не предлагать реализованное как новую идею.
5. Не переписывать согласованную архитектуру/дизайн без технической или продуктовой причины.
6. Если пользователь указал конкретное направление, оно имеет приоритет над общим roadmap.

После существенной работы обновить секцию **Checkpoint** ниже.

## Definition of Done

Функция считается завершённой, когда:
- основной сценарий работает;
- предусмотрены loading/empty/error/retry/disabled/success состояния;
- права и trust boundaries проверены на сервере;
- нет утечки секретов или лишних персональных данных;
- работает в Telegram WebView и на основных мобильных размерах;
- не ломает дизайн-систему;
- есть автоматическая или воспроизводимая проверка;
- затронутая документация обновлена.

## Checkpoint

**Дата:** 2026-09-14.
**Что сделано:** финансовый hardening вынесен в `codex/staffswipe-financial-hardening` поверх `codex/staffswipe-release-candidate`: проверяемое пополнение ЮKassa, exactly-once зачисление, атомарные денежные изменения, реальные provider refunds с локальным резервом, durable `request_id`, reconciliation для `pending`/`succeeded`/`rejected`/`unknown`, безопасная GET-сверка по `provider_refund_id`, ограничение повторного POST безопасным окном, защита удаления аккаунта с финансовыми обязательствами, Alembic migration и регрессионные тесты SQLite/PostgreSQL.
**Проверено:** head `0424c8973f04f9057689282d93f881fa4baaa4cb`: Backend CI — lint/FastAPI success и PostgreSQL success; E2E — success; Security — success по pip-audit, npm audit, gitleaks, CodeQL Python, CodeQL JavaScript/TypeScript и critical gate. Это автоматические проверки; реальные операции в ЮKassa и production-инфраструктура этим не подтверждаются.
**Открытые программные задачи:** идемпотентная отправка сообщений и доступ к активным WebSocket; черновики/ошибки/история чата; доступность свайпов и reduced motion; согласованный статус админки и документов; дальнейшая доводка UX обеих ролей и trust/safety сценариев.
**Внешние условия запуска:** настройки Telegram и платёжного провайдера, проверка webhook/возвратов на тестовом контуре ЮKassa, инфраструктура, настоящие устройства iOS/Android, резервное копирование и доступ единственного владельца. Реальные платежи этим checkpoint не подтверждаются.
**Следующая конкретная задача:** после review/интеграции financial hardening закрыть переписку: exactly-once/idempotent send, активные WebSocket, retry/error/draft/history, затем прогнать backend + PostgreSQL + frontend/E2E + Security и только после этого переходить к следующему release-кандидату.
