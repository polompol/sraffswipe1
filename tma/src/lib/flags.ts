// Пилотный режим: прячет второстепенные фичи, чтобы старт был максимально
// простым (меньше отвлекающего для первых пользователей и оператора).
//
// Что прячем при VITE_PILOT_MODE=true:
//  - верификацию компании по ИНН (DaData) — первые кафе приводятся лично,
//    и ключа к сервису проверки на пилоте может не быть вовсе.
// По умолчанию ВКЛЮЧЁН (пилот — простое ядро). Полный набор возвращается
// явным VITE_PILOT_MODE=false.
export const PILOT_MODE = import.meta.env.VITE_PILOT_MODE !== "false";
