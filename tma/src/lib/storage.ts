/**
 * Ключи браузерного хранилища — в одном месте.
 *
 * Раньше строки вроде "ss_city" писались прямо в коде экранов: одиннадцать
 * ключей в девяти файлах. Опечатка в таком ключе ничего не ломает громко —
 * приложение просто «забывает» город или тему, и найти это можно только
 * руками. Здесь же видно весь список того, что мы храним на телефоне.
 *
 * Ничего личного в хранилище не кладём: только выбор человека (город, вид
 * ленты, тема, звук, крупный шрифт) и то, без чего не войти (токен, роль).
 */
export const LS = {
  /** Токен входа. */
  jwt: "ss_jwt",
  /** Роль: соискатель или заведение. */
  role: "ss_role",
  /** Свой id — им отличаем свои сообщения в чате от чужих. */
  uid: "ss_uid",
  /** Согласие с офертой и обработкой данных. */
  consent: "ss_consent",
  /** Город ленты: выбор человека сильнее города из анкеты. */
  city: "ss_city",
  /** Вид ленты: карточками или списком. */
  view: "ss_view",
  /** Подсказку про свайп показываем один раз. */
  swipeHinted: "ss_swipe_hinted",
  /** Тема: светлая или тёмная. */
  theme: "ss_theme",
  /** Звук выключен. */
  soundOff: "ss_sound",
  /** Крупные кнопки и текст. */
  large: "ss_large",
  /** Последнее известное место — чтобы не спрашивать заново при каждом входе. */
  geo: "ss_geo",
} as const;

/** То, что живёт только до закрытия приложения. */
export const SS = {
  /** Приложение уже открывали в этот заход — приветствие показываем один раз. */
  opened: "ss_open",
} as const;
