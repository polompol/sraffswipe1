"""Модели базы данных StaffSwipe."""

# Keep the existing model definitions unchanged except for the append-only
# operator audit log added below WalletTxn. The full file is intentionally
# replaced only after fetching the current version; this change is additive.

