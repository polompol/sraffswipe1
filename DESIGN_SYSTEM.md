# Дизайн-система StaffSwipe

Источник правды — **`tma/src/theme/theme.css`** (токены) и **`tma/src/index.css`**
(компоненты). Этот файл только объясняет правила словами. Если код и документ
разошлись — прав код, а документ надо чинить.

> Прежняя версия этого файла описывала другой продукт: приложение на Flutter с
> оранжевым `#FF6B35` и розовым `#F72585`, «супер-лайком» и виджетами Dart.
> Так проект выглядел на этапе замысла — до того, как стал мини-приложением
> в Telegram на React. Документ никто не обновил, и он год вводил в
> заблуждение всех, кто его открывал: по нему выходило, что у StaffSwipe
> безликая палитра из шаблона, хотя в приложении давно другая.

---

## Что делает StaffSwipe узнаваемым

**Багровый на слоновой кости.** Не синий, как большинство мини-приложений
Telegram, и не тёмный с неоново-оранжевым, как все сервисы подработки разом.
Тёплый светлый фон работает там, где приложением пользуются: кухня, зал,
солнце в окне, взгляд на две секунды между делами.

**Карточка смены — главный объект.** Она занимает почти весь экран, на ней
крупно сумма за смену, должность, время, расстояние и фотография места.
Всё остальное в интерфейсе — обслуга этой карточки.

**Свайп — фирменное движение.** Вправо «хочу», влево «мимо»; карточка
физически улетает, а не «отправляется форма».

---

## Цвета

Все цвета — токены из `theme.css`. Прямых значений в коде быть не должно:
иначе тёмная тема и крупный режим перестают работать на этом месте.

| Токен | Светлая | Тёмная | Для чего |
| --- | --- | --- | --- |
| `--gold` | `#a51c30` | `#d24b60` | Бренд, акцентный текст, ссылки |
| `--gold-fill` | `#a51c30` | `#a02338` | Заливка под белым текстом |
| `--bg` | `#efe7d3` | `#160d0f` | Фон экрана (слоновая кость / винный) |
| `--surface` `--card` | `#fffdf8` | `#201316` `#28171b` | Карточки и панели |
| `--text` | `#241f1b` | `#f3e7e9` | Основной текст |
| `--muted` | `#6d6357` | `#b8a7ab` | Пояснения, подписи |
| `--border` / `--border-strong` | `#e4d9c2` / `#8d7c66` | `#3a2329` / `#7d6169` | Разделители / рамки кнопок |
| `--success` | `#2f7a4d` | `#5ec98a` | «Смена закрыта» |
| `--danger` / `--danger-bg` | `#7e1322` | светлее | Ошибка, спор |
| `--super` / `--super-text` | `#c39a3a` / `#7d5f18` | `#d8b85a` | Золотые акценты, «проверено» |

Два правила, которые легко нарушить и трудно заметить:

1. **Акцентный цвет и заливка — разные токены.** В тёмной теме `--gold`
   осветлён, чтобы читаться текстом; заливать им кнопку с белой надписью
   нельзя — контраст падает до нечитаемого. Для заливок есть `--gold-fill`.
2. **Смысловые цвета отдельно от бренда.** «Получилось» не должно быть цвета
   кнопки: успех — зелёный, спор и ошибка — тёмно-багровый.

---

## Типографика

Шрифт системный (`-apple-system` / `SF Pro` / `Inter` / `Roboto`): в
мини-приложении Telegram он загружен всегда и не стоит ни килобайта.

Размеры — только из шкалы, числом в разметке их быть не должно:

| Токен | Размер | Где |
| --- | --- | --- |
| `--text-xs` | 13px | Нижняя граница. Мельче в приложении нет ничего |
| `--text-sm` | 14px | Подписи, второстепенное |
| `--text-base` | 15px | Основной размер интерфейса |
| `--text-md` | 16px | Поля ввода, крупные кнопки |
| `--text-lg` | 19px | Заголовки блоков |
| `--text-xl` | 22px | Заголовки экранов |
| `--text-2xl` | 26px | Название заведения на карточке |
| `--text-3xl` | 30px | Заголовок страницы |
| `--text-display` | 34px | Должность на карточке кандидата |
| `--text-hero` | 46px | Сумма за смену — один раз на экран |

**Крупный режим** (переключатель в профиле, для слабого зрения) поднимает всю
шкалу разом — поэтому размеры и берутся из неё. Пока их писали числами,
режим увеличивал только половину надписей, и человек не понимал, почему
помогло наполовину.

---

## Кнопки, подписи, зоны нажатия

**Кнопка — это `<button>` с рамкой или заливкой. Подпись — это `<span>` с
мягкой заливкой без рамки.** Правило держится само собой и не требует помнить
классы: `span.tag` в CSS специально выглядит иначе, чем `button.tag`. До этого
подписи «готов сегодня», «Опытный», «На карту в день» рисовались точно так же,
как кнопки рядом, — по ним жали, и ничего не происходило.

**Одна заливная кнопка на экран.** Остальные действия — контуром, ссылкой или
под «Ещё». Заливка цветом означает «главное действие здесь»; когда их три,
это не значит ничего.

**Минимум 44 пикселя** в высоту у всего, что нажимают. Это проверяется
тестом (`tma/src/theme/typography.test.ts`) вместе с нижней границей текста.

Готовые стили: `.btn` (54px, заливка), `.btn.secondary` (поверхность с
рамкой), `.btn.ghost` (только рамка), `.tag` (чип 44px), `.icon-btn` (44×44),
`.act` (круглые кнопки свайпа, 72px), компонент `Button` с размерами
`sm/md/lg` и защитой от двойного нажатия.

---

## Формы и радиусы

| Токен | Значение | Для чего |
| --- | --- | --- |
| `--radius-sm` | 12px | Кнопки, поля ввода |
| `--radius` | 16px | Карточки, листы |
| `--radius-lg` | 24px | Карточка смены |
| `--radius-pill` | 999px | Чипы, аватары |

Отступы — шкала `--space-1…7` (4, 8, 12, 16, 24, 32, 48). Тени — три уровня
`--elev-1…3`; выше третьего ничего не поднимаем.

---

## Движение

Анимации короткие и объясняют, что произошло:

- свайп карточки — пружина, карточка улетает в сторону выбора;
- нажатие — уменьшение до 0.97 (кнопка) и 0.9 (круглая кнопка свайпа);
- мэтч — оверлей с золотом, один раз и без повторов;
- закрытие смены — денежный дождь: единственный праздничный момент, и он
  привязан к тому, ради чего всё затевалось.

Всё, что дольше 800 мс, — уже задержка, а не украшение. При включённом
`prefers-reduced-motion` анимации выключаются.

---

## Тёмная тема

Не инверсия, а своя палитра: винный фон `#160d0f`, карточки `#28171b`,
осветлённый багровый для текста. Проверяется отдельно — в тёмной теме легко
получить нечитаемый белый текст на светлой заливке или наоборот.

Тема переключается в профиле и хранится в `ss_theme`; каркас-заставка в
`index.html` читает тот же ключ, чтобы при запуске не мигало белым.

---

## Доступность

- Контраст текста к фону — не ниже 4.5:1, у крупного — не ниже 3:1.
  Приглушённый текст (`--muted`) специально тёмный ради этого.
- Видимый фокус с клавиатуры у всех кнопок и полей.
- Каждая иконка-кнопка имеет `aria-label` с тем же текстом, который видно.
- Крупный режим и тёмная тема — не «галочки», а рабочие режимы: экраны
  проверяются в обоих.

---

## Как проверять

```sh
cd tma && npm run lint && npx tsc --noEmit && npx vitest run && npm run build
```

Тесты дизайн-системы: `src/theme/typography.test.ts` — размеры только из
шкалы, ничего мельче 13px, зоны нажатия не меньше 44px.
